unit ScanCheckOut;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, GridsEh, DBGridEh, ExtCtrls, DB, DBTables, StdCtrls, ComCtrls,DateUtils,Comobj,
  Menus;

type
  TScanCheckOut1 = class(TForm)
    DBGridEh1: TDBGridEh;
    Panel1: TPanel;
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    DTP1: TDateTimePicker;
    DTP2: TDateTimePicker;
    Edit1: TEdit;
    Button1: TButton;
    Button2: TButton;
    Query1: TQuery;
    DataSource1: TDataSource;
    Query1YSBH: TStringField;
    Query1ROut: TIntegerField;
    Query1OutDate: TDateTimeField;
    Query1SuppName: TStringField;
    Query2: TQuery;
    PopupMenu1: TPopupMenu;
    Detail1: TMenuItem;
    procedure FormShow(Sender: TObject);
    procedure FormDestroy(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure FormCreate(Sender: TObject);
    procedure Button1Click(Sender: TObject);
    procedure Button2Click(Sender: TObject);
    procedure Detail1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  ScanCheckOut1: TScanCheckOut1;

implementation

{$R *.dfm}

uses main1,FunUnit,DetailOut;

procedure TScanCheckOut1.FormDestroy(Sender: TObject);
begin
ScanCheckOut1:=nil;
end;


procedure TScanCheckOut1.FormClose(Sender: TObject; var Action: TCloseAction);
begin
action:=cafree;
ScanCheckOut1:=nil;
end;

procedure TScanCheckOut1.FormShow(Sender: TObject);
begin
  DTP1.Date := Now;
end;

procedure TScanCheckOut1.FormCreate(Sender: TObject);
var NDate:TDate;
begin
  NDate := Date;  // ho?c Now n?u mu?n c? gi?
  DTP1.Date := NDate;
  DTP2.Date := NDate;
end;

procedure TScanCheckOut1.Button1Click(Sender: TObject);
begin
 with query1 do
  begin
    active:=false;
    sql.Clear;
    sql.add(' select YSBH, SUM (Rout) as Rout, MAX(OutDate) as OutDate, SuppName from TB_DD.dbo.SMDDScanOut ');
    sql.add('where TB_DD.dbo.SMDDScanOut.YSBH like '+''''+'%'+Edit1.Text+'%'+'''');
    sql.add('   and    convert(varchar,OutDate,111) between  ');
    sql.add('          '''+formatdatetime('yyyy/MM/dd',DTP1.Date)+''' and '''+formatdatetime('yyyy/MM/dd',DTP2.Date)+'''  ');
    sql.add(' and ROut > 0 ');
    sql.add('group by YSBH, SuppName');
  //  showmessage(Query1.SQL.text) ;
    //funcObj.WriteErrorLog(sql.Text);
    active:=true;
  end;
end;

procedure TScanCheckOut1.Button2Click(Sender: TObject);
var eclApp,WorkBook:olevariant;
    i,j:integer;
begin
 if  query1.active  then
    begin
      try
        eclApp:=CreateOleObject('Excel.Application');
        WorkBook:=CreateOleObject('Excel.Sheet');
      except
        Application.MessageBox('No Microsoft   Excel','Microsoft   Excel',MB_OK+MB_ICONWarning);
        Exit;
      end;
      try
          WorkBook:=eclApp.workbooks.Add;
          for   i:=0   to   query1.fieldcount-1   do
            begin
                eclApp.Cells(1,i+1):=query1.fields[i].FieldName;
            end;

          query1.First;
          j:=2;
          while   not   query1.Eof   do
            begin
              for   i:=0   to  query1.fieldcount-1  do
              begin
                eclApp.Cells(j,i+1):=query1.Fields[i].Value;
              end;
            query1.Next;
            inc(j);
            end;
         eclapp.columns.autofit;
         showmessage('Succeed');
         eclApp.Visible:=True;
      except
          on   F:Exception   do
            showmessage(F.Message);
      end;
    end;

end;


procedure TScanCheckOut1.Detail1Click(Sender: TObject);
begin
  DetailOut1:=TDetailOut1.create(self);
  DetailOut1.show;
end;

end.
