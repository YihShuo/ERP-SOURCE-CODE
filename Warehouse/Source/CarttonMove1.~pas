unit CarttonMove1;

interface

uses
  {Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, ComCtrls, StdCtrls, ExtCtrls, Grids, DBGrids, DB, DBTables; }
  
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, Grids, DBGrids, StdCtrls, ExtCtrls, ComCtrls, DB, DBTables,comobj,
  Menus;

type
  TCarttonMove = class(TForm)
    Panel1: TPanel;
    Label3: TLabel;
    Label6: TLabel;
    Label5: TLabel;
    Label1: TLabel;
    Label8: TLabel;
    Edit3: TEdit;
    Edit2: TEdit;
    Button1: TButton;
    Edit1: TEdit;
    Button2: TButton;
    DTP1: TDateTimePicker;
    DTP2: TDateTimePicker;
    SCSMCK: TQuery;
    DS1: TDataSource;
    DBGrid1: TDBGrid;
    SCSMCKDDBH: TStringField;
    SCSMCKQty: TFloatField;
    SCSMCKXieXing: TStringField;
    SCSMCKShehao: TStringField;
    SCSMCKArticle: TStringField;
    SCSMCKXieMing: TStringField;
    SCSMCKDDQty: TIntegerField;
    SCSMCKshipdate: TDateTimeField;
    CheckBox1: TCheckBox;
    SCSMCKCTQty: TIntegerField;
    SCSMCKCTDD: TIntegerField;
    PopupMenu1: TPopupMenu;
    Detail1: TMenuItem;
    SCSMCKQtyLack: TIntegerField;
    SCSMCKCTLack: TIntegerField;
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure Button1Click(Sender: TObject);
    procedure FormCreate(Sender: TObject);
    procedure Button2Click(Sender: TObject);
    procedure Edit1KeyPress(Sender: TObject; var Key: Char);
    procedure Edit2KeyPress(Sender: TObject; var Key: Char);
    procedure Edit3KeyPress(Sender: TObject; var Key: Char);
    procedure Detail1Click(Sender: TObject);
    procedure SCSMCKCalcFields(DataSet: TDataSet);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  CarttonMove: TCarttonMove;

implementation

uses CarttonMove_Det1;

{$R *.dfm}

procedure TCarttonMove.FormClose(Sender: TObject;
  var Action: TCloseAction);
begin
action:=cafree;
end;

procedure TCarttonMove.Button1Click(Sender: TObject);
begin
with SCSMCK do
  begin
    if not checkbox1.Checked then
      begin
        active:=false;
        sql.Clear;
        sql.add('select SCSMCK.DDBH,count(SCSMCK.CTNO) as CTQty,isnull(sum(SCSMRK.Qty),0) as Qty,XXZL.XieXing,');
        sql.add('SCZLDate.CTQty as CTDD,XXZL.Shehao,XXZL.Article,XXZL.XieMing,DDZL.Pairs as DDQty,DDZL.shipdate');
        sql.add('from SCSMCK ');
        sql.add('left join SCSMRK on SCSMCK.DDBH=SCSMRK.DDBH and SCSMCK.CTNO=SCSMRK.CTNO ');
        sql.add('left join DDZL on SCSMCK.DDBH=DDZL.DDBH ');
        sql.add('left join XXZl on XXZL.XieXing=DDZl.XieXing and XXZL.SheHao=DDZL.SheHao ');
        sql.add('left join SCZLDate on SCZLDate.ZLBH=DDZL.ZLBH ');
        sql.add('where SCSMCK.DDBH like '+''''+edit3.text+'%'+'''');
        sql.add(' and XXZL.Article like '+''''+edit1.Text+'%'+'''');
        sql.add(' and XXZL.XieMing like '+''''+'%'+edit2.Text+'%'+'''');  
        sql.add(' and Convert(smalldatetime,convert(varchar,SCSMCK.USERDate,111)) between ');
        sql.add(''''+formatdatetime('yyyy/MM/dd',DTP1.date)+'''');
        sql.add(' and '+''''+formatdatetime('yyyy/MM/dd',DTP2.date)+'''');
        sql.add('group by  SCSMCK.DDBH,XXZL.XieXing,XXZL.Shehao,XXZL.Article,XXZL.XieMing,DDZL.Pairs,DDZL.shipdate,SCZLDate.CTQty');
        sql.add('order by SCSMCK.DDBH ');
        active:=true;
      end
      else
        begin
          active:=false;
          sql.Clear;
          sql.add('select SCSMCK.DDBH,isnull(sum(SCSMRK.Qty*SCSMCK.CKCS),0) as Qty,XXZL.XieXing,XXZL.Shehao,XXZL.Article,');
          sql.add('isnull(sum(SCSMCK.CKCS),0) as CTQty,XXZL.XieMing,DDZL.Pairs as DDQty,DDZL.shipdate,SCZLDate.CTQty as CTDD');
          sql.add('from SCSMCK ');
          sql.add('left join SCSMRK on SCSMCK.DDBH=SCSMRK.DDBH and SCSMCK.CTNO=SCSMRK.CTNO ');
          sql.add('left join DDZL on SCSMCK.DDBH=DDZL.DDBH ');
          sql.add('left join XXZl on XXZL.XieXing=DDZl.XieXing and XXZL.SheHao=DDZL.SheHao ');
          sql.add('left join SCZLDate on SCZLDate.ZLBH=DDZL.ZLBH ');
          sql.add('where SCSMCK.DDBH like '+''''+edit3.text+'%'+'''');
          sql.add(' and XXZL.Article like '+''''+edit1.Text+'%'+'''');
          sql.add(' and XXZL.XieMing like '+''''+'%'+edit2.Text+'%'+'''');
          sql.add(' and Convert(smalldatetime,convert(varchar,SCSMCK.USERDate,111)) between ');
          sql.add(''''+formatdatetime('yyyy/MM/dd',DTP1.date)+'''');
          sql.add(' and '+''''+formatdatetime('yyyy/MM/dd',DTP2.date)+'''');
          sql.add('group by  SCSMCK.DDBH,XXZL.XieXing,XXZL.Shehao,XXZL.Article,XXZL.XieMing,DDZL.Pairs,DDZL.shipdate,SCZLDate.CTQty');
          sql.add('order by SCSMCK.DDBH ');
          active:=true;
        end;
  end;

end;

procedure TCarttonMove.FormCreate(Sender: TObject);
begin
DTP1.Date:=Date-6;
DTP2.Date:=date;
end;

procedure TCarttonMove.Button2Click(Sender: TObject);
var
      eclApp,WorkBook:olevariant;
 //     xlsFileName:string;
      i,j:integer;
begin
if SCSMCK.Active then
  begin
    if SCSMCK.recordcount=0 then
      begin
        showmessage('No record.');
        abort;
      end;
  end
  else
    begin
      showmessage('No record.');
      abort;
    end;

try
  eclApp:=CreateOleObject('Excel.Application');
  WorkBook:=CreateOleObject('Excel.Sheet');
except
  Application.MessageBox('NO Microsoft   Excel','Microsoft   Excel',MB_OK+MB_ICONWarning);
  Exit;
end;

try
  WorkBook:=eclApp.workbooks.Add; 
  eclApp.Cells(1,1):='NO';
  for   i:=1   to   SCSMCK.fieldcount   do
    begin
      eclApp.Cells(1,i+1):=SCSMCK.fields[i-1].FieldName;
    end;
  SCSMCK.First;
  j:=2;
  while   not  SCSMCK.Eof   do
    begin
      eclApp.Cells(j,1):=j-1;
      for   i:=1   to   SCSMCK.fieldcount   do
        begin
          eclApp.Cells(j,i+1):=SCSMCK.Fields[i-1].Value;
          eclApp.Cells.Cells.item[j,i+1].font.size:='8';
        end;
      SCSMCK.Next;
      inc(j);
    end;
  eclapp.columns.autofit;
  showmessage('Succeed.');
  eclApp.Visible:=True;
except
  on   F:Exception   do
    showmessage(F.Message);
end;

end;

procedure TCarttonMove.Edit1KeyPress(Sender: TObject; var Key: Char);
begin
if key=#13 then
  edit2.setFocus;
end;

procedure TCarttonMove.Edit2KeyPress(Sender: TObject; var Key: Char);
begin

if key=#13 then
  edit3.setFocus;
end;

procedure TCarttonMove.Edit3KeyPress(Sender: TObject; var Key: Char);
begin

if key=#13 then
  button1.setFocus;
end;

procedure TCarttonMove.Detail1Click(Sender: TObject);
begin
CarttonMove_Det:=TCarttonMove_Det.create(self);
CarttonMove_Det.show;
end;

procedure TCarttonMove.SCSMCKCalcFields(DataSet: TDataSet);
begin
SCSMCK.FieldByName('QtyLack').Value:=SCSMCK.FieldByName('DDQty').Value-SCSMCK.FieldByName('Qty').Value;
SCSMCK.FieldByName('CTLack').Value:=SCSMCK.FieldByName('CTDD').Value-SCSMCK.FieldByName('CTQty').Value;
end;

end.
